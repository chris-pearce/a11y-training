<!DOCTYPE html>
<html {{#if section}} class="{{section}}" {{/if}}>
  <head>
    <meta charset="utf-8">
    <title>
      {{#if title}}
        {{title}} | {{siteName}}
      {{else}}
        {{siteName}}
      {{/if}}
    </title>
    <meta name="viewport" content="width=device-width, initial-scale=1">
    <link rel="stylesheet" href="/assets/css/index.css">
  </head>
  <body>
    {{> includes/header}}

    {{> content}}
  </body>
</html>
