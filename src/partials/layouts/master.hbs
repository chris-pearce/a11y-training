<!DOCTYPE html>
<html {{#if section}} class="p-{{section}}" {{/if}} lang="en-au">
  <head>
    <meta charset="utf-8">
    <title>
      {{#if title}}
        {{title}} | {{siteName}}
      {{else}}
        {{siteName}}
      {{/if}}
    </title>
    <meta name="viewport" content="width=device-width, initial-scale=1">
    <link rel="stylesheet" href="/assets/css/index.css">
  </head>
  <body>
    {{> layouts/header}}

    <main class="l-main">
      {{> layouts/main}}
    </main>
  </body>
</html>
