<header class="header">
  <h1><a href="/">{{siteName}}</a></h1>
  <nav>
    <ul>
      {{#each siteSection}}
        <li>
          <a href="/{{url}}" {{#if section}} class="active" {{/if}}>{{title}}</a>
        </li>
      {{/each}}
    </ul>
  </nav>
</header>
